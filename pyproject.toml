[project]
name = "about-you-search-reranking-case-study"
version = "0.1.0"
description = "ABOUT YOU data scientist case study — search reranking"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=2.2",
    "pyarrow>=15",
]

[tool.uv]
package = false
